<Window x:Class="V0AISongwritingApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:V0AISongwritingApp"
        mc:Ignorable="d"
        Title="V0 AI Songwriting App" Height="900" Width="1600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Grid>
        <!-- 3D glassmorphic bento-box grid layout -->
        <Grid.Effect>
            <BlurEffect Radius="32"/>
        </Grid.Effect>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!-- Bento-boxes (sample) -->
        <Border Grid.Row="1" Grid.Column="1" CornerRadius="32" Background="#AA1A1A2E" BorderBrush="#FF00FFF7" BorderThickness="2" Effect="{DynamicResource GlassEffect}">
            <TextBlock Text="Welcome to V0 AI Songwriting" Foreground="#FF00FFF7" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <!-- More bento-boxes and UI elements will be added here -->
    </Grid>
</Window>
